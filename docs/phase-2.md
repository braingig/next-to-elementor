# Phase 2 — IR Schema + Fixture Corpus

Status: **complete for review**  
Depends on: Phase 0 contracts, Phase 1 Free catalog (untouched)

## Delivered

- IR schema `0.2.0` (`lib/converter/ir/schema.ts`)
- Deterministic normalization (`lib/converter/ir/normalize.ts`)
- Fixture corpus (`tests/converter/fixtures/ir/`)
- Validation tests (`tests/converter/ir.test.ts`)
- Documentation (`docs/ir.md`)

## Explicitly out of Phase 2

- JSX/TSX parsing
- React → IR conversion
- IR → Elementor conversion
- Elementor JSON generation
- Catalog changes

## Next

**Phase 3** — only after review approval.
