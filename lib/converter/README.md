# Converter

- **Phase 0:** IR / report / decision contracts (`ir/`, `report/`, `types/`).
- **Phase 1:** Elementor Free capability catalog (`catalog/`) — data + load + compliance only.

Do **not** add JSX parsing, style resolution, conversion rules, or Elementor JSON emission until the corresponding phases.

Specs: `/docs` (`phase-0.md`, `phase-1.md`, `ir.md`, `catalog.md`, …).

Public exports: `./index.ts`
